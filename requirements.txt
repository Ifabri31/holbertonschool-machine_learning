# holbertonschool-machine_learning
# Entorno de corrección (según guía del proyecto):
#   Ubuntu 20.04 LTS, Python 3.9, numpy 1.25.2, pandas 2.2.2, pycodestyle 2.11.1
# Instalación (recomendado crear el venv con python3.9):
#   python3.9 -m venv .venv && source .venv/bin/activate
#   pip install -U pip && pip install -r requirements.txt

# --- Versiones fijadas por el programa ---
numpy==1.25.2
pandas==2.2.2
pycodestyle==2.11.1

# --- Ciencia de datos / ML clásico (imports en math/, clustering, árboles, etc.) ---
scipy==1.11.4
matplotlib==3.8.4
scikit-learn==1.4.2

# --- Deep learning (TensorFlow + Keras para scripts que usan `tensorflow` o `keras`) ---
tensorflow==2.15.1
keras==2.15.0
protobuf>=4.21.6,<5

# --- Visión (YOLO / cv2) ---
opencv-python==4.9.0.80

# --- NLP y datasets ---
gensim==4.3.2
tensorflow-hub==0.15.0
# 4.9.4+ exige Python >=3.10; con 3.9 la última compatible es 4.9.3.
tensorflow-datasets==4.9.3
transformers==4.36.2

# --- Refuerzo (q_learning: gymnasium) ---
gymnasium==0.29.1
